# # Web framework
# fastapi==0.111.0
# uvicorn[standard]==0.29.0

# # Database — PostgreSQL
# sqlalchemy==2.0.30
# alembic==1.13.1
# psycopg2-binary==2.9.9          # PostgreSQL adapter

# # Auth / Security
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4
# python-multipart==0.0.9

# # Validation / Settings
# pydantic[email]==2.7.1
# pydantic-settings==2.2.1

# # HTTP client (tests / health checks)
# httpx==0.27.0

# # Production WSGI/ASGI process manager
# gunicorn==22.0.0











# Curio Cloud Quiz — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework ──────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==21.2.0              # production WSGI/ASGI server

# ── Database ───────────────────────────────────────────────────
sqlalchemy==2.0.30
psycopg2-binary==2.9.9        # PostgreSQL driver (binary = no compile needed)
alembic==1.13.1               # database migrations

# ── Auth / security ────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4             # password hashing
python-multipart==0.0.9            # form data (OAuth2PasswordRequestForm)

# ── Config ────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1      # Settings with .env support
python-dotenv==1.0.1

# ── Email (OTP / password reset) ──────────────────────────────
# Uses stdlib smtplib — no extra package needed

# ── File uploads (avatar) ─────────────────────────────────────
python-multipart==0.0.9       # already listed above, just noting it enables UploadFile

# ── Utilities ─────────────────────────────────────────────────
httpx==0.27.0                 # optional: for testing async endpoints
email-validator==2.1.1        # validates EmailStr in Pydantic schemas
